# Direct packages used by the validated Python 3.12 CPU maintenance environment.
# Choose a compatible PyTorch build for a CUDA training machine.
torch==2.14.0
torchvision==0.29.0
numpy==2.5.3
matplotlib==3.11.1
Pillow==12.3.0
tqdm==4.70.0
jupyterlab>=4,<5
